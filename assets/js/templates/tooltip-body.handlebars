{{#if isType}}
  <section class="docstring docstring-type">
    {{#if isBuiltInType}}
      Built-in type defined as <code>{{this.hint.description}}</code>
    {{else}}
      {{this.hint.description}}
    {{/if}}
  </section>
{{else}}
  <div class="detail-header">
      {{#if this.isModule}}
        <h2>{{this.hint.title}}</h2>
      {{else}}
        <h1 class="signature">
          {{this.hint.title}}
          <div class="specs">{{this.hint.signatureSpecs}}</div>
        </h1>
      {{/if}}
  </div>
  {{#if this.hint.description}}
    <section class="docstring">
      {{this.hint.description}}
    </section>
  {{/if}}
{{/if}}
