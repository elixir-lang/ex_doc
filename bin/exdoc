#!/usr/bin/env elixir
Code.prepend_path File.expand_path("../../ebin", __FILE__)

files = File.wildcard 'samples/for_docs/*'

docs = EDocs.get_docs(files, [])

EDocs.generate_markdown(docs)

# vim: ft=elixir:
