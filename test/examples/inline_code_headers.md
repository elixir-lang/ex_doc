# Inline Code in Headers Demo

## The `File` module

### Using `GenServer` callbacks

#### The `handle_call/3` function

##### `start_link/1` options

This page demonstrates the issue where inline code in headers appears with inconsistent sizing.

## `Enum` module overview

The code element above is in an h2 header — it should match the heading font size.

### `String.split/2` and `String.join/2`

These are common string functions.
